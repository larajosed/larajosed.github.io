# Jose Daniel Lara Meza | Portfolio
My name is Jose Daniel and I'm a junior web developer.\
This portfolio is meant to show my profile, training and studies, goals and side projects to the world.\
Please, feel free to access my portfolio's website at https://larajosed.github.io/ and contact me if needed.\
HOPE YOU LIKE IT!!
___
>Author: Jose Daniel Lara (larajosed@gmail.com)\
>Developed using **VueJS 3.0**\
>Deployed by **GitHub Pages**

## 🚀 Development Guide / Guía de Desarrollo

### Running the Project Locally / Ejecutar el Proyecto Localmente

To see changes in your local development environment:

**English:**
1. Install dependencies: `npm install`
2. Start the development server: `npm run dev`
3. Open your browser at the URL shown (usually `http://localhost:5173`)
4. Your changes will appear automatically with hot-reload

**Español:**
1. Instala las dependencias: `npm install`
2. Inicia el servidor de desarrollo: `npm run dev`
3. Abre tu navegador en la URL mostrada (usualmente `http://localhost:5173`)
4. Tus cambios aparecerán automáticamente con recarga en caliente

### Building and Deploying / Construcción y Despliegue

To deploy changes to GitHub Pages, you need to build the project:

**English:**
1. Build the project: `npm run build` (creates the `dist/` folder)
2. Copy to deployment folder: `npm run cpdir` (copies `dist/` to `docs/`)
3. Or use the combined command: `npm run build-and-deploy`
4. Commit and push the changes to the `docs/` folder to GitHub

**Español:**
1. Construye el proyecto: `npm run build` (crea la carpeta `dist/`)
2. Copia a la carpeta de despliegue: `npm run cpdir` (copia `dist/` a `docs/`)
3. O usa el comando combinado: `npm run build-and-deploy`
4. Haz commit y push de los cambios en la carpeta `docs/` a GitHub

### Important Notes / Notas Importantes

**English:**
- The development server (`npm run dev`) serves files from the `src/` folder
- The production build creates optimized files in the `dist/` folder
- GitHub Pages serves files from the `docs/` folder
- After making code changes, you must run `npm run build-and-deploy` and push to see them on the live site

**Español:**
- El servidor de desarrollo (`npm run dev`) sirve archivos de la carpeta `src/`
- La construcción de producción crea archivos optimizados en la carpeta `dist/`
- GitHub Pages sirve archivos de la carpeta `docs/`
- Después de hacer cambios en el código, debes ejecutar `npm run build-and-deploy` y hacer push para verlos en el sitio en vivo